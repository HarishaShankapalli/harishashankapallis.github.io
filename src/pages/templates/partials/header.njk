<header>
  <h1>{{ site.siteName }}</h1>
  <p>{{ site.tagline }}</p>
  <nav>
    <ul>
      {% for item in site.nav %}
        <li><a href="{{ item.url }}">{{ item.title }}</a></li>
      {% endfor %}
    </ul>
  </nav>
</header>
